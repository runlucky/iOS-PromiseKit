# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'iOS-PromiseKit' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod "PromiseKit", "~> 6.0"

  # Pods for iOS-PromiseKit

  target 'iOS-PromiseKitTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'iOS-PromiseKitUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
